= render partial: 'dashboard/projects/project_form'
.view-projects-main
  - @projects && @projects.each do |project|
    = render partial: 'dashboard/projects/project_show', locals: { project: project }
