.config-container
  %h4 Display Images
  .settings-form-container#change-image-display
    %p Select which images will be displayed on your portfolio.  What replaces the image will depend on your chosen theme.
    = form_for [current_user, @portfolio] do |f|
      .form-image-display
        .field
          = f.label :show_avatar_image
          = f.select :show_avatar_image, [['Yes', true], ['No', false]], selected: @portfolio.show_avatar_image
        .field
          = f.label :show_cover_image
          = f.select :show_cover_image, [['Yes', true], ['No', false]], selected: @portfolio.show_cover_image
        .actions
          = submit_tag 'Update', class: 'update-button'
